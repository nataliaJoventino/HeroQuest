## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ##
## >> >> >> >> ## -- -- -- -- -- ## >> >> >> >> >> >> >> ## -- -- ##
## >> >> >> >> ## -- -- -- -- -- ## >> >> >> >> >> >> >> ## -- -- ##
## >> >> >> >> ## -- -- -- -- -- ## >> >> >> >> >> >> >> ## -- -- ##
## >> >> >> >> ## -- -- -- -- -- ## ## ## ## ## ## ## ## ## -- -- ##
## >> >> >> >> ## -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- ##
## >> >> >> >> ## -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- ##
## >> >> >> >> ## -- -- -- ## ## ## ## ## ## -- -- ## ## ## ## ## ##
## ## ## ## ## ## -- -- -- ## >> >> >> >> ## -- -- ## >> >> >> >> ##
## >> >> >> >> ## -- -- -- ## >> >> >> >> ## -- -- ## >> >> >> >> ##
## >> >> >> >> ## -- -- -- ## >> >> >> >> ## -- -- ## >> >> >> >> ##
## >> >> >> >> ## -- -- -- ## >> >> >> >> ## -- -- ## >> >> >> >> ##
## >> >> >> >> ## -- -- -- ## >> >> >> >> ## -- -- ## >> >> >> >> ##
## >> >> >> >> ## -- -- -- ## ## ## ## ## ## -- -- ## ## ## ## ## ##
## >> >> >> >> ## -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- ##
## >> >> >> >> ## -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- ##
## ## ## ## ## ## -- -- -- ## ## ## ## ## ## ## ## ## ## ## ## ## ##
## -- -- -- -- -- -- -- -- ## >> >> >> >> ## >> >> >> >> >> >> >> ##
## -- -- -- -- -- -- -- -- ## >> >> >> >> ## >> >> >> >> >> >> >> ##
## -- -- -- -- -- -- -- -- ## >> >> >> >> ## >> >> >> >> >> >> >> ##
## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ## ##